enum UploadType {
  GALLERY,
  CAMERA,
}

enum PostCaptionOpenMode {
  FROM_POST,
  FROM_FEED,
}

enum emailOpenMode {
  EMAIL_REGISTER,
  EMAIL_LOGIN,
}

enum FeedMode {
  FROM_FEED, //自分＋　フォローしてる人画面
  FROM_PROFILE //プロフィール画面に表示されている人のみ
}

enum PostMenu {
  EDIT,
  DELETE,
  SHARE
}